let initialState = {
  currentUser: {
    id: "2323",
    username: "Krismata",
  },
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
